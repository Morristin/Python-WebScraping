[project]
name = "WebScraping"
version = "0.0.10"
description = "Collect and analyse the good's prices."
requires-python = ">=3.14"
dependencies = [
    "selenium >= 4.43.0",
    "beautifulsoup4 >= 4.14.3",
]

[tool.setuptools.packages.find]
include = ["web_scraping*", "database*"]